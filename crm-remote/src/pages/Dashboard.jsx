import React from "react";

export default function Dashboard() {
  return (
    <div className="section">
      <h2>CRM Remote</h2>
      <p className="muted">
        This remote owns its own nested routes. Try navigating between Dashboard, Accounts, and
        Settings without leaving the host shell.
      </p>
    </div>
  );
}
